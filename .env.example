# Control Panel Port (for /status, /list, etc.)
CONTROL_PORT=11001

# Proxy Server Port (main proxy port)
PROXY_PORT=11000

# Server public hostname/IP (for display in /list)
SERVER_HOST=217.15.163.20

# Proxy API Configuration
PROXY_API_URL=http://217.15.163.20:8549/api/cron/getliveproxiesdataserver?authensone=mysonetrend&time=60

# Update interval in milliseconds (30000 = 30 seconds)
UPDATE_INTERVAL=30000

# Proxy Authentication (Username-based)
PROXY_PASSWORD=mypass
USERNAME_PREFIX=proxy

# OLD CONFIG (for multi-port mode)
# START_PROXY_PORT=11001
# ENABLE_AUTH=false
# AUTH_USERNAME=admin
# AUTH_PASSWORD=changeme123
