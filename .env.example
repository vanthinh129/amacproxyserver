# Control Panel Port (for /status, /list, /dashboard)
CONTROL_PORT=11001

# Proxy Server Port (main proxy port for username-based authentication)
PROXY_PORT=11000

# Proxy API Configuration
PROXY_API_URL=http://217.15.163.20:8549/api/cron/getliveproxiesdata?authensone=mysonetrend&time=60

# Update interval in milliseconds (30000 = 30 seconds)
UPDATE_INTERVAL=30000

# Proxy Authentication (Username-based)
# Users will connect like: curl -x http://proxy1:mypass@host:8080
PROXY_PASSWORD=mypass
USERNAME_PREFIX=proxy
